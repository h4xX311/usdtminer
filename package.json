[tooling]
# This file intentionally minimal. Use `npm install` to populate node_modules.
{
  "name": "usdtminer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint . --ext .js",
    "test": "echo \"No tests configured yet.\" && exit 0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
